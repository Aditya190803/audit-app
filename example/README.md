# Example Data Policy

Do not commit real bank statements, client lists, exports, or other customer data
under this directory.

Use `example/synthetic/` only for deliberately fake fixtures that are safe to
publish. Private parser samples should stay outside git.
